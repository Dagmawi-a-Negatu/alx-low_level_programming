#include <stdio.h>
#include "main.h"
#include <ctype.h>

/**
 * mul- checks if character is uppercase
 * @a: First operand
 * @b: second operand
 * Return: Return a multiplied by b
 */
int mul(int a, int b)
{
	return (a * b);
}
