#include "main.h"
#include <string.h>
/**
 * _strlen- write a function that returs length of string
 * @s: value to be checked
 *
 * Return: return length number of pointer variable
 */

int _strlen(char *s)
{
	int count;

	count = strlen(s);

	return (count);
}
