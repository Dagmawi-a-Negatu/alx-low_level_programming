#include <stdio.h>

/**
 * main - Print the alphabet in lowercase
 *
 * Return: 0 for main interger return functionality
 */

int main(void)
{

	int i;

	for (i = 'a'; i <= 'z'; i++)
		putchar(i);
		putchar('\n');
	return (0);
}
