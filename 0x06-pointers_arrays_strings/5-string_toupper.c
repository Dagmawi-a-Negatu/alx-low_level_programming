#include "main.h"
#include <string.h>
#include <ctype.h>

/**
 * *string_toupper - return all uppercase letters
 * @*: string to be returned
 * Return: string
 */
char *string_toupper(char *i)
{
	return (i);
}
