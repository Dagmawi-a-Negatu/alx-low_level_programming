#include "main.h"
#include <string.h>
#include <ctype.h>

/**
 * reverse_array- reverses an array
 * *: char to b
 * Return: string
 */
char *string_toupper(char *l)
{
	char *str;

	return ((char *) toupper((long int) l));
}
